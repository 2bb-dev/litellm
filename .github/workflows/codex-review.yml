name: Codex Review Bot
on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review
        required: true
        type: number

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get PR info
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_NUMBER=${{ github.event.pull_request.number || inputs.pr_number }}
          BASE=$(gh pr view $PR_NUMBER --repo ${{ github.repository }} --json baseRefName --jq '.baseRefName')
          HEAD=$(gh pr view $PR_NUMBER --repo ${{ github.repository }} --json headRefName --jq '.headRefName')
          echo "number=$PR_NUMBER" >> $GITHUB_OUTPUT
          echo "base=$BASE" >> $GITHUB_OUTPUT
          echo "head=$HEAD" >> $GITHUB_OUTPUT

      - name: Get PR diff
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr diff ${{ steps.pr.outputs.number }} --repo ${{ github.repository }} > /tmp/pr.diff
          echo "Diff size: $(wc -l < /tmp/pr.diff) lines"

      - name: Run Codex review via LiteLLM
        env:
          LITELLM_KEY: ${{ secrets.LITELLM_KEY }}
          LITELLM_URL: ${{ secrets.LITELLM_BASE_URL }}
        run: |
          python3 - <<'EOF'
          import os, json
          from urllib.request import urlopen, Request

          diff = open("/tmp/pr.diff").read()
          if not diff.strip():
              open("/tmp/review.md", "w").write("No changes to review.")
              exit(0)

          prompt = f"""You are a code reviewer. Review the following PR diff and provide structured feedback.

## Review Format
1. **Summary** — one paragraph assessment
2. **Issues** — bugs, security problems, edge cases (severity: HIGH/MEDIUM/LOW)
3. **Suggestions** — specific improvements
4. **Verdict** — APPROVE / REQUEST_CHANGES / COMMENT

## PR Diff
```diff
{diff[:12000]}
```"""

          body = json.dumps({
              "model": "openai/gpt-5.3-codex",
              "messages": [{"role": "user", "content": prompt}],
              "max_tokens": 2000
          }).encode()

          req = Request(
              f"{os.environ['LITELLM_URL']}/chat/completions",
              data=body,
              headers={
                  "Authorization": f"Bearer {os.environ['LITELLM_KEY']}",
                  "Content-Type": "application/json"
              }
          )
          resp = json.loads(urlopen(req).read())
          review = resp["choices"][0]["message"]["content"]
          open("/tmp/review.md", "w").write(review)
          EOF

      - name: Post review as PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const review = fs.readFileSync('/tmp/review.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number || ${{ steps.pr.outputs.number }},
              body: `### Codex Review\n\n${review}`
            });
